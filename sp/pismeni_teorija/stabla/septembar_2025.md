<img width="1153" height="221" alt="image" src="https://github.com/user-attachments/assets/718f98c7-2461-4c95-b279-d4a9f0332679" />

<br><br>

## **1️⃣ Статичка и динамичка меморијска репрезентација бинарног стабла**

### **(a) Статичка репрезентација**

Бинарно стабло се може представити **секвенцијално**, тј. у низу.
Ово се често користи када је стабло **потпуно (complete binary tree)** — попут хипа.

👉 **Идеја:**

* Корен је на позицији `1` (или `0`, зависно од нотације).
* Лево дете чвора на позицији `i` налази се на позицији `2i`.
* Десно дете — на позицији `2i + 1`.
* Родитељ чвора на позицији `i` је на позицији `⌊i/2⌋`.

**Пример:**

```
Index:  1  2  3  4  5  6  7
Value: 10 15 20 30 40 50 60
```

→ Корен: `10`
→ Лево дете корена (index 2): `15`
→ Десно дете корена (index 3): `20`, итд.

---

### **(b) Динамичка репрезентација**

Ово је најчешћи начин представљања бинарног стабла.

Сваки чвор је структура (рекорд) са три поља:

```text
struct Node {
    value
    left  → pointer на лево дете
    right → pointer на десно дете
}
```

Графички:

```
      [ value ]
      /       \
   left       right
```

Корен стабла је показивач `root` који показује на први чвор.

---

## **2️⃣ Формирање бинарног стабла тражења (BST) из низа**

Низ је:
**A = (1, 5, 6, 2, 7, 3, 9, 4)**

👉 У BST сваки елемент који убацујемо иде:

* у **лево подстабло** ако је мањи од вредности у чвору,
* у **десно подстабло** ако је већи.

---

### **Поступак уметања и изглед стабла након сваког корака**

#### 1. Убацујемо 1:

```
1
```

#### 2. Убацујемо 5 → веће од 1 → иде десно

```
   1
    \
     5
```

#### 3. Убацујемо 6 → веће од 1, веће од 5 → десно од 5

```
   1
    \
     5
      \
       6
```

#### 4. Убацујемо 2 → веће од 1, мање од 5 → лево од 5

```
   1
    \
     5
    / \
   2   6
```

#### 5. Убацујемо 7 → веће од 1, 5, 6 → десно од 6

```
   1
    \
     5
    / \
   2   6
        \
         7
```

#### 6. Убацујемо 3 → веће од 1, мање од 5, веће од 2 → десно од 2

```
    1
     \
      5
     / \
    2   6
     \   \
      3   7
```

#### 7. Убацујемо 9 → веће од 1, 5, 6, 7 → десно од 7

```
    1
     \
      5
     / \
    2   6
     \   \
      3   7
           \
            9
```

#### 8. Убацујемо 4 → веће од 1, мање од 5, веће од 2, веће од 3 → десно од 3

```
    1
     \
      5
     / \
    2   6
     \   \
      3   7
       \   \
        4   9
```

✅ **Коначни BST:**

```
        1
         \
          5
         / \
        2   6
         \   \
          3   7
           \   \
            4   9
```

---

## **3️⃣ Формирање MinHeap-а из низа A = (1,5,6,2,7,3,9,4)**

MinHeap има особину:

* Родитељ ≤ деца
* Дрво је потпуно бинарно

**Поступак:** убацујемо елементе један по један и одржавамо heap особину (bubble up).

---

### 1. Убацујемо 1:

```
1
```

### 2. Додајемо 5:

```
   1
  /
 5
```

✅ Добро (1 < 5)

### 3. Додајемо 6:

```
    1
   / \
  5   6
```

✅ Добро (1 < 5,6)

### 4. Додајемо 2:

```
      1
     / \
    5   6
   /
  2
```

→ 2 < 5 → размењујемо

```
      1
     / \
    2   6
   /
  5
```

### 5. Додајемо 7:

```
      1
     / \
    2   6
   / \
  5   7
```

✅ Добро (све задовољава)

### 6. Додајемо 3:

```
      1
     / \
    2   6
   / \  /
  5  7 3
```

→ 3 < 6 → размена:

```
      1
     / \
    2   3
   / \  /
  5  7 6
```

### 7. Додајемо 9:

```
      1
     / \
    2   3
   / \  / \
  5  7 6  9
```

✅ Добро

### 8. Додајемо 4:

```
        1
       / \
      2   3
     / \  / \
    5  7 6  9
   /
  4
```

→ 4 < 5 → размена

```
        1
       / \
      2   3
     / \  / \
    4  7 6  9
   /
  5
```

✅ **Коначни MinHeap:**

```
        1
       / \
      2   3
     / \  / \
    4  7 6  9
   /
  5
```

---

## **4️⃣ Псеудокод за Trazi(S, E)** (секвенцијална репрезентација BST)

Ако је стабло задато низом `S`, где:

* Корен је на позицији 1
* Лево дете је `2i`
* Десно дете је `2i + 1`

---

### **Псеудокод:**

```
Funkcija Trazi(S, E, i)
    ako i > duzina(S) ili S[i] = NULL onda
        vrati "Nema"
    ako S[i] = E onda
        vrati i
    ako E < S[i] onda
        vrati Trazi(S, E, 2*i)
    inace
        vrati Trazi(S, E, 2*i + 1)
Kraj
```

**Позив:** `Trazi(S, E, 1)` — почињемо од корена.
Враћа индекс чвора или "Nema" ако елемент није у стаблу.


